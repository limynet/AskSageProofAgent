# Runtime dependencies for the AskSage Proof Agent Dash app (dashboard.py).
#
# These are the packages actually imported by the running app and its engine,
# verified against the working .venv (dash 4.4.1, plotly 7.0.0, flask 3.1.3,
# gunicorn 26.2.0, openai 3.13.0, PyPDF2 3.0.1, python-docx 1.2.0).
#
# The Dash version is pinned because the UI (canvas via markup strings,
# clientside callbacks) was built and tested against 4.4.x.

dash>=4.4.1
plotly>=7.0.0
flask>=3.1
gunicorn>=26.0
openai>=3.0
python-dotenv>=1.0
PyPDF2>=3.0
python-docx>=0.8.11
